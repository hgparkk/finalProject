<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.codingbamboo.finalproject.carboncalculate.dao.ICarbonCalculateDAO">
	<insert id="insertCal" parameterType="CarbonCalculateDTO">
		INSERT INTO carbon_result(
			result_no
			, user_id
			, result_date
			, electric_usage
			, gas_usage
			, traffic_kind
			, traffic_value
			, waste_weight
		) VALUES(
			re_seq.NEXTVAL
			, #{userId}
			, #{resultDate, jdbcType=DATE}
			, #{electricUsage}
			, #{gasUsage}
			, #{trafficKind}
			, #{trafficValue}
			, #{wasteWeight}
		)
	</insert>
	
	<select id="selectCalList" parameterType="String" resultType="CarbonCalculateDTO">
		SELECT
			result_no
			, user_id
			, result_date
			, electric_usage
			, gas_usage
			, traffic_kind
			, traffic_value
			, waste_weight
		FROM
			carbon_result
		WHERE 1=1
			AND user_id = #{userId}
	</select>
</mapper>
